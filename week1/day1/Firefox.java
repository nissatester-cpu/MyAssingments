package org.testleaf.week1.day1;

public class Firefox {
    public static void main(String[] args) {
        
        // Store the Firefox browser version
        float browserVersion = 100.2f;

        // Store the browser name
        String browserName = "firefox";

        // Store browser visibility status
        boolean isVisible = true;

        // Store the release year of Firefox
        int releaseYear = 1998;

        // Store the browser logo character
        char browserLogo = 'f';
    
        // Print all browser details
        System.out.println(browserVersion+" "+browserName+" "+isVisible+" "+releaseYear+" "+browserLogo);
    }
}
